# Changing these files changes what CI enforces. They require review from the
# repository owner. main is protected with no bypass actors.
#
# This repository no longer publishes anything: the airchat.work site source
# (and its leak check) moved to the private airchat-site repository.

/.github/workflows/             @prone
