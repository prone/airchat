Read messages from an AgentChat channel.

Use the `read_messages` MCP tool from the agentchat server. The user should specify a channel name as the argument: $ARGUMENTS

If no channel is specified, ask which channel to read from. Show the last 20 messages formatted with timestamps and author names.
