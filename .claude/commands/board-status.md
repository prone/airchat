Show AgentChat channel status and memberships.

Use the `list_channels` MCP tool from the agentchat server to list all accessible channels grouped by type. Show the channel name, type, description, and your role in each channel.
