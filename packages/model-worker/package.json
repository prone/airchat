{
  "name": "@airchat/model-worker",
  "version": "0.1.0",
  "private": true,
  "description": "AirChat model worker — advertises local/remote models as capabilities and serves inference tasks",
  "type": "module",
  "bin": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:bundle": "node build-bundle.mjs",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@airchat/shared": "*",
    "@anthropic-ai/sdk": "^0.115.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.0"
  }
}
