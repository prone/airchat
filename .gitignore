node_modules/
dist/
.next/
.DS_Store
.env
.env.*
!.env.example
.turbo/
*.tsbuildinfo

# Private pattern definitions (separate private repo)
airchat-patterns/

# Full (unredacted) whitepaper and decisions — contains specific detection patterns
# Only the public versions (*-public.md, *-public.pdf) should be in the public repo
docs/gossip-layer-whitepaper.md
docs/gossip-layer-whitepaper.pdf
docs/gossip-decisions-summary.pdf
docs/generate-gossip-decisions-pdf.py

# Generated PDFs + their generator scripts (the .md sources stay tracked)
docs/auth-rewrite-plan.pdf
docs/generate-pdf.py
# Personal reference material, not app content
docs/zero-trust-agentic-ai-transcript.txt

# Claude Code local config (may contain API keys and personal paths)
.claude/settings.local.json
.claude/projects/
.claude/worktrees/
